#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: always 0 (A successful code will return 0)
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
