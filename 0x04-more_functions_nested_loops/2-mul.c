#include "main.h"
#include <stdio.h>

/**
 * mul - multiply two numbers
 * @a: first parameter to be multiplied
 * @b: second parameter to be multiplied
 * Return: product
 */

int mul(int a, int b)
{
	return (a * b);
}
